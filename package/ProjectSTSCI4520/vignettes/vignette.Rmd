---
title: "vignette"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{vignette}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(ProjectSTSCI4520)
```

```{r}
# map of the interpolated average temperature of stations in march 2024
march_2024_data <- daily_weather |> dplyr:::filter(LST_DATE >= "2024-03-01", LST_DATE < "2024-04-01") |> dplyr:::group_by(`station name`, LONGITUDE, LATITUDE) |> dplyr:::summarise(avg_temp = mean(T_DAILY_AVG))
grid <- create_grid(resolution_X = 1000, resolution_Y=1000)
interp_data = interpolate_data(march_2024_data$avg_temp, march_2024_data$`station name`, grid)
plot_interpolations(interp_data)
```
